fun main() {
    // Subset with single element not in superset - should be false
    val r = set_is_subset(Set["x"], Set["y"])
    print("subset ({x}) of ({y}): ")
    print(r)
    println("")

    println("done")
}
