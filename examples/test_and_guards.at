// Test: and guards in when
fun main() {
    val x = 5
    val result = when x {
        n and n < 0 -> "negative"
        n and n > 0 -> "positive"
        else -> "zero"
    }
    println(result)
    println("done")
}
